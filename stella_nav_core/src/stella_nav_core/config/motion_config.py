
class MotionConfig(object):
    def __init__(self, max_speed, min_speed, max_accel, samples):
        self.max_speed = max_speed
        self.min_speed = min_speed
        self.max_accel = max_accel
        self.samples = samples
